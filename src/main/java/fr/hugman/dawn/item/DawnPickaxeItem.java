package fr.hugman.dawn.item;

import net.minecraft.item.ToolMaterial;

public class DawnPickaxeItem extends net.minecraft.item.PickaxeItem {
	/* Extension for internal publicity */
	public DawnPickaxeItem(ToolMaterial material, int attackDamage, float attackSpeed, Settings settings) {
		super(material, attackDamage, attackSpeed, settings);
	}
}
